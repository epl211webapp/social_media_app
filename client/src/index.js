/*
Index.js is is the main file of the application, where the actual application is rendered. 
This is like the main for other  

*/

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
